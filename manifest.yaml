---
name: tn3270-ng2
id: org.zowe.terminal.tn3270
# Without the v
version: 2.18.0
# Human readable component name
title: TN3270
# Human readable component description
description: 'Basic TN3270 Display Emulator'
homepage: https://zowe.org
keywords:
  - zlux
  - appfw
  - app
  - angular
  - tn3270
  - terminal
license: EPL-2.0
repository:
  type: git
  url: https://github.com/zowe/tn3270-ng2.git
# we do not specify encoding here because its already tagged ascii
build:
  branch: "{{build.branch}}"
  number: "{{build.number}}"
  commitHash: "{{build.commitHash}}"
  timestamp: "{{build.timestamp}}"
appfwPlugins:
  - path: .
